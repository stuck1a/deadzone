###############################################################
###   DEAZONE ROADMAP FÜR DAS PACKAGE "deadzone.graphics"   ###
###############################################################


- Fixen, dass nur das letzte Objekt (das letzte VBO) gerendert wird.
  Dafür liegen zwei funktionierende Varianten (alles in einem VBO und jedes Objekt bzw. jedes Triangle eines Objekts in jeweils einem eigenen VBO) in den Klassen
  TestForOpenGl_XXX.java die den Ablauf prozedural wiedergeben. Aus Performancesicht sollte unbedingt die Variante mit mehreren VBOs implementiert werden.
  => Aufjedenfall muss das funktionierende Trivial-Beispiel auf die reale Applikation angewendet werden. Vergleichen, was falsch läuft usw



- RectangleShape



- Uniforms in die Shader einfügen damit nicht bei jedem Loop alles komplett neu in die GPU geladen werden muss.
  So wie ich das verstanden habe kann man mit uniforms die bereits im GPU-Speicher liegenden Vertexdaten manipulieren,
  indem man die MVP-Faktoren in die Shader einführt.
